# Uygulama
APP_ENV=development
LOG_LEVEL=INFO
CORS_ORIGINS=http://localhost:3000

# Database (DB entegrasyonu hazır olunca aktif edilecek)
# DATABASE_URL=postgresql+psycopg://finans:finans@localhost:5432/finans

# Vector DB (RAG entegrasyonunda aktif edilecek)
# VECTOR_DB_URL=http://localhost:8001
# EMBEDDING_MODEL=

# LLM (mimari kararı sonrası doldurulacak)
LLM_API_KEY=
DEFAULT_MODEL=

# Agent bazlı model konfigürasyonu (boş bırakılırsa DEFAULT_MODEL kullanılır)
PORTFOLIO_MODEL=
MARKET_MODEL=
RISK_MODEL=